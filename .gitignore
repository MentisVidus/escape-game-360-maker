# OS / éditeurs
Thumbs.db
Desktop.ini
.DS_Store
*.swp
*~

# Dépendances (Chemin B : npm install depuis xflow/react/)
node_modules/
xflow/react/node_modules/
# Build Vite (xflow/react) — ne pas versionner : régénérer avec `npm run build:editor-map`
xflow/react/dist/

# Fichiers locaux de travail (jeux générés / exports)
# Décommentez si vous ne voulez pas versionner certains dossiers :
# dist/
# *.local.json

# Notes / pistes / brainstorming non versionnés (locales seulement)
docs/temporaire/

# Claude Code (local machine) — ne pas versionner
.claude/
